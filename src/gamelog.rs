/// The Gamelog struct holds a simple vector of strings containing all of the messages 
/// that we have sent to be printed for the player to see.
pub struct GameLog {
    pub entries : Vec<String>
}
